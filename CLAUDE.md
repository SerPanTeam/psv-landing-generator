# Claude Code Instructions: Landing Generator System

## 🎯 Цель проекта

Создать систему генерации лендингов для фотографов на основе Bootstrap 5, состоящую из переиспользуемых секций. Система должна обеспечивать:
- **Pixel-perfect** соответствие Figma макетам (в рамках Bootstrap)
- **CSS переменные** для кастомизации каждого лендинга
- **Переиспользование** одинаковых секций
- **SEO оптимизацию**
- **Адаптивность** для всех устройств

---

## 📁 Структура проекта

```
landing-generator/
├── sections/                     # HTML-шаблоны секций
│   ├── header/
│   │   └── header-v1.html
│   ├── hero/
│   │   ├── hero-v1.html         # Лендинг 1
│   │   ├── hero-v2.html         # Лендинг 2
│   │   ├── hero-v3.html         # Лендинг 3
│   │   └── hero-v4.html         # Лендинг 4
│   ├── promo/
│   │   └── promo-v1.html
│   ├── features/
│   │   ├── features-3col.html
│   │   └── features-2col.html
│   ├── steps/
│   │   └── steps-4.html
│   ├── gallery/
│   │   ├── gallery-strip.html
│   │   ├── gallery-slider.html
│   │   └── gallery-fullwidth.html
│   ├── cta/
│   │   └── cta-v1.html
│   ├── faq/
│   │   └── faq-accordion.html
│   ├── services/
│   │   └── services-grid.html
│   ├── about/
│   │   └── about-v1.html
│   ├── footer/
│   │   └── footer-v1.html
│   └── quiz/
│       ├── quiz-step.html       # Универсальный шаг квиза
│       ├── quiz-form.html       # Форма сбора данных
│       └── quiz-success.html    # Страница успеха
├── assets/
│   ├── images/
│   │   └── placeholder.svg      # SVG плейсхолдер
│   └── icons/
│       ├── facebook.svg
│       ├── instagram.svg
│       └── web.svg
├── css/
│   ├── variables.css            # CSS переменные (главный файл кастомизации)
│   ├── base.css                 # Базовые стили, типографика
│   ├── components.css           # Кнопки, карточки, формы
│   ├── sections.css             # Стили всех секций
│   ├── quiz.css                 # Стили квиза
│   └── responsive.css           # Адаптивные стили
├── js/
│   ├── quiz.js                  # Логика квиза
│   ├── faq.js                   # FAQ аккордеон
│   ├── gallery-slider.js        # Логика слайдера галереи
│   └── analytics.js             # Заготовка для аналитики
├── config/
│   ├── landing1-family.json
│   ├── landing2-family-kids.json
│   ├── landing3-dogs.json
│   └── landing4-kids.json
├── dist/                        # Готовые HTML файлы
├── generator.js                 # Скрипт сборки
├── package.json
└── README.md
```

---

## 🔗 Figma ссылки

### Лендинг 1: Familien-Fotoshooting (Weihnachten)
```
Главная: node-id=1-3
Quiz Step 1: node-id=137-51
Quiz Step 2: node-id=137-159
Quiz Step 3: node-id=137-71
Quiz Step 4: node-id=137-179
Quiz Form: node-id=137-111
Quiz Success: node-id=137-84
```

### Лендинг 2: Familien und Kinder-Fotoshooting (Kostenlos)
```
Главная: node-id=233-22
Quiz Step 1: node-id=313-26
Quiz Step 2: node-id=313-82
Quiz Step 3: node-id=313-47
Quiz Step 4: node-id=313-68
Quiz Form: node-id=313-100
Quiz Success: node-id=313-135
```

### Лендинг 3: Hunde-Fotoshooting
```
Главная: node-id=369-256
Quiz Step 1: node-id=370-441
Quiz Step 2: node-id=370-460
Quiz Step 3: node-id=370-479
Quiz Step 4: node-id=370-503
Quiz Form: node-id=370-526
Quiz Success: node-id=370-560
```

### Лендинг 4: Kinder-Fotoshooting
```
Главная: node-id=479-30
Quiz Step 1: node-id=483-63
Quiz Step 2: node-id=483-85
Quiz Step 3: node-id=483-100
Quiz Step 4: node-id=483-122
Quiz Form: node-id=483-144
Quiz Success: node-id=483-180
```

**Figma File Key**: `qGiP8ZP2Ot8Tut1faJDl4p`

---

## 🎨 Дизайн-система

### Цвета (CSS переменные)
```css
:root {
  /* Основные цвета */
  --color-bg-primary: #F5EDE0;
  --color-bg-secondary: #EEE3D0;
  --color-text-primary: #3D3D3D;
  --color-button: #E2C08D;
  --color-button-hover: #D4A574;
  --color-button-text: #FFFFFF;
  --color-white: #FFFFFF;
  --color-black: #000000;
  
  /* Акценты */
  --color-accent: #D4A574;
  --color-error: #FF6B6B;
  --color-success: #4CAF50;
  
  /* Границы */
  --color-border: #3D3D3D;
  --color-border-light: rgba(61, 61, 61, 0.2);
}
```

### Типографика
```css
:root {
  --font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
  
  /* Desktop */
  --font-size-h1: 55px;
  --font-size-h2: 45px;
  --font-size-h3: 35px;
  --font-size-text-lg: 28px;
  --font-size-text: 22px;
  --font-size-small: 16px;
  
  /* Font weights */
  --font-weight-bold: 700;
  --font-weight-medium: 500;
  --font-weight-regular: 400;
}
```

### Отступы и размеры
```css
:root {
  --container-max-width: 1240px;
  --section-padding-y: 80px;
  --content-gap: 40px;
  --content-gap-sm: 20px;
  
  --button-height: 70px;
  --button-padding-x: 40px;
  --button-border-radius: 0;
  
  --card-border-radius: 0;           /* Лендинги 1, 2 */
  --card-border-radius-rounded: 20px; /* Лендинги 3, 4 */
}
```

---

## 📋 Порядок разработки

### ФАЗА 1: Инфраструктура
1. Создать структуру папок
2. Настроить package.json
3. Создать CSS файлы с переменными
4. Создать базовые стили

**Команды Figma для получения данных:**
```
Figma:get_design_context fileKey=qGiP8ZP2Ot8Tut1faJDl4p nodeId=1:3
Figma:get_variable_defs fileKey=qGiP8ZP2Ot8Tut1faJDl4p nodeId=1:3
```

### ФАЗА 2: Общие компоненты
1. Header (логотип)
2. Footer (карта + контакты + соцсети)
3. Кнопки (primary, outline)
4. Карточки квиза (2 варианта: square, rounded)
5. Форма (inputs, select, checkbox)

### ФАЗА 3: Секции лендинга
Для каждой секции:
1. Получить данные из Figma: `Figma:get_design_context`
2. Создать HTML шаблон с {{placeholders}}
3. Добавить CSS стили
4. Проверить соответствие макету

**Секции по приоритету:**
1. Hero (4 варианта)
2. Features (3 колонки)
3. Steps (4 шага)
4. FAQ (аккордеон)
5. About
6. Services Grid
7. Promo
8. Gallery Strip
9. CTA

### ФАЗА 4: Квиз-система
1. Quiz Step (универсальный компонент)
2. Quiz Form
3. Quiz Success
4. JavaScript логика переключения

### ФАЗА 5: Сборка лендингов
1. Создать JSON конфиги для каждого лендинга
2. Написать generator.js
3. Собрать все 4 лендинга
4. Добавить SEO мета-теги

### ФАЗА 6: Адаптивность
1. Mobile styles (< 768px)
2. Tablet styles (768px - 992px)
3. Desktop optimization

### ФАЗА 7: Финализация
1. Проверить все лендинги
2. Оптимизировать код
3. Подготовить для GitHub Pages

---

## 🔧 Технические требования

### Bootstrap 5
- Использовать Bootstrap grid (container, row, col-*)
- Использовать Bootstrap utilities где возможно
- Переопределять стили через CSS переменные

### HTML шаблоны
Формат placeholders:
```html
<h1 class="heading-1">{{title}}</h1>
<p class="text-regular">{{description}}</p>
<a href="{{buttonLink}}" class="btn-primary">{{buttonText}}</a>
```

### CSS организация
```css
/* sections.css */

/* ===== HERO ===== */
.hero { ... }
.hero--v1 { ... }  /* Вариант для лендинга 1 */
.hero--v2 { ... }  /* Вариант для лендинга 2 */

/* ===== FEATURES ===== */
.features { ... }
```

### JavaScript
- Vanilla JS (без фреймворков)
- ES6+ синтаксис
- Модульная структура

---

## ⚠️ Важные правила

1. **ВСЕГДА** использовать CSS переменные для цветов, размеров, шрифтов
2. **НИКОГДА** не хардкодить значения в HTML (только через классы)
3. **Bootstrap grid** для layout, кастомные классы для стилей
4. **Pixel-perfect** — сверяться с Figma через get_screenshot
5. **Переиспользование** — одинаковые секции = один шаблон
6. **Mobile-first** — начинать с мобильных стилей

---

## 📊 Чеклист готовности

### Для каждой секции:
- [ ] HTML шаблон создан
- [ ] CSS стили добавлены
- [ ] Адаптивность настроена
- [ ] Соответствует Figma макету

### Для каждого лендинга:
- [ ] JSON конфиг создан
- [ ] HTML собран
- [ ] Квиз работает
- [ ] SEO мета-теги добавлены
- [ ] Проверен на всех breakpoints

### Финальная проверка:
- [ ] Все 4 лендинга собраны
- [ ] Формы работают (console.log)
- [ ] Аналитика готова к подключению
- [ ] GitHub Pages деплой работает

---

## 🚨 100% ДЕТЕКТОР ПРОБЛЕМ — ОБЯЗАТЕЛЬНЫЙ ЧЕКЛИСТ

**ПЕРЕД КАЖДОЙ секцией выполни ВСЕ пункты. Пропуск = баг.**

### 1️⃣ ФОНЫ — Проверь ПЕРВЫМ ДЕЛОМ!
```
□ Получи screenshot всей страницы: get_screenshot nodeId=XXX:XX (главная страница)
□ Определи цвет фона КАЖДОЙ секции визуально
□ Сравни с дефолтами шаблонов (см. таблицу ниже)
□ Добавь bgClass если фон отличается от дефолта
```

**Дефолты шаблонов:**
| Шаблон | Default | Если нужен другой → |
|--------|---------|---------------------|
| `hero-v1/v2/v3` | primary | — |
| `hero-v4` | dark #3D3D3D | — |
| `content-v1/v2` | primary | — |
| `cta-v1` | **secondary** | `bgClass: "section--bg-primary"` |
| `steps-4` | **secondary** | `bgClass: "section--bg-primary"` |
| `steps-v4` | primary | — |
| `faq-accordion` | primary | `bgClass: "section--bg-secondary"` |
| `services-grid` | **secondary** | `bgClass: "section--bg-primary"` |
| `about-v1` | primary | — |
| `gallery-slider` | primary | — |

### 2️⃣ КНОПКИ — Проверь ВСЕ параметры!
```
□ Получи design_context для секции с кнопкой
□ Найди кнопку по "bg-[#e2c08d]" или "Rounded rectangle"
□ Запиши: left=?, width=?, height=?, rounded=?
□ Определи выравнивание: left/center/right?
□ Кнопка ВНУТРИ колонки или ОТДЕЛЬНО?
```

**Частые ошибки с кнопками:**
| Проблема | Как найти в Figma | Решение |
|----------|-------------------|---------|
| Кнопка по центру вместо слева | `left: 47-54px` = слева | `text-align: left` |
| Кнопка справа под картинкой | `left: calc(50%+...)` | Поместить в правую колонку |
| Неправильный размер | `w-[Xpx] h-[Ypx]` | Точные width/height в CSS |
| Неправильный radius | `rounded-[Xpx]` | `border-radius: Xpx` |

### 3️⃣ ПОЗИЦИОНИРОВАНИЕ ЭЛЕМЕНТОВ
```
□ Для каждого элемента запиши top и left из Figma
□ Элементы с ОДИНАКОВЫМ top = в одной строке (row)
□ Элементы с РАЗНЫМ top = вертикально (column)
□ Кнопка ПОД чем-то? → поместить в ту же колонку/контейнер
```

**Пример анализа:**
```
Title:  top=1394, left=46   → левая колонка
Text:   top=1394, left=735  → правая колонка (тот же top = рядом)
Button: top=1491, left=47   → левая колонка ПОД title (разный top, тот же left)
```

### 4️⃣ ОТСТУПЫ (margins)
```
□ Вычисли отступ: element2.top - element1.top - element1.height
□ Запиши в CSS с комментарием: /* FIGMA: X - Y = Zpx */
□ Проверь margin-bottom vs margin-top — что логичнее?
```

### 5️⃣ HEADER/LOGO
```
□ Есть ли ОТДЕЛЬНЫЙ header или лого ВНУТРИ секции?
□ Landing 4: лого ВНУТРИ hero (не отдельный header!)
□ Проверь top координату лого — если < 100px и на тёмном фоне → внутри hero
```

### 6️⃣ ФЛАГИ ШАБЛОНОВ — Проверь ВСЕ!
```
□ rounded: true/false — скруглённые углы?
□ cardStyle: "rounded" — для FAQ/quiz
□ alignLeft: true — левое выравнивание?
□ bgClass: "..." — переопределение фона?
□ imagesRight: true — картинки справа?
□ layoutRow: true — горизонтальный layout?
```

### 7️⃣ ФИНАЛЬНАЯ ПРОВЕРКА
```
□ Запусти node generator.js
□ Открой HTML в браузере
□ Сравни ВИЗУАЛЬНО с Figma screenshot
□ Проверь: фоны, отступы, выравнивание кнопок, размеры
```

### 8️⃣ ПЕРЕНОСЫ СТРОК — Проверь ВИЗУАЛЬНО!
```
□ Получи screenshot из Figma
□ Посчитай СКОЛЬКО СТРОК занимает каждый текстовый элемент
□ Определи: текст на ОДНОЙ строке или переносится ЕСТЕСТВЕННО?
□ Если в Figma текст на ОДНОЙ строке → добавь `white-space: nowrap`
□ Если текст переносится естественно по ширине → НЕ добавляй nowrap
□ Проверь на desktop и mobile breakpoints
```

**Частые ошибки:**
| Figma | Реализация | Проблема | Решение |
|-------|------------|----------|---------|
| 1 строка | 2+ строки | Текст переносится | `white-space: nowrap` |
| Естественный перенос | `nowrap` | Текст обрезается | Убрать nowrap |
| 2 строки разного размера | 1 элемент | Неправильная структура | Разбить на 2 элемента |

**Пример ошибки:** Quiz-form Landing 3 - title "Trag dich jetzt ein und sichere dir dein" должен быть на ОДНОЙ строке, но CSS не имел `white-space: nowrap`.

### 9️⃣ ЗАКРУГЛЕНИЯ КАРТИНОК — Проверь ВСЕ изображения!
```
□ Получи design_context или screenshot для каждой секции с картинками
□ Найди параметр `rounded-[Xpx]` или `border-radius` в Figma
□ Landing 1/2: обычно БЕЗ закругления (border-radius: 0)
□ Landing 3/4: обычно С закруглением (border-radius: 40px)
□ Проверь: шаблон поддерживает флаг `rounded: true`?
□ Добавь `rounded: true` в JSON конфиг если нужно
```

**Секции с поддержкой `rounded: true`:**
| Шаблон | Элемент | Класс | Border-radius |
|--------|---------|-------|---------------|
| `quiz-page-form.html` | form image | `.quiz-form__image--rounded` | 40px |
| `quiz-page-success.html` | success image | `.quiz-success__image--rounded` | 40px |
| `about-v1.html` | about image | `.about--rounded .about__image` | 40px |
| `services-grid.html` | card images | `.services--rounded .service-card__image` | 40px |
| `gallery-fullwidth.html` | gallery image | `.gallery-fullwidth--rounded` | 40px |
| `steps-v4.html` | steps image | автоматически 40px | 40px |

**Пример ошибки:** Quiz-success Landing 3 - изображение было БЕЗ закругления, потому что в конфиге success не было `rounded: true`.

---

## ✅ Чеклист соответствия Figma

При создании/обновлении секций **ОБЯЗАТЕЛЬНО** проверять каждый пункт:

### Галерея
- [ ] Галерея - **слайдер** или **статика**? (используй `gallery-slider` или `gallery-strip`)
- [ ] Есть ли **fullwidth галерея** между секциями? (используй `gallery-fullwidth`)

### Выравнивание
- [ ] Выравнивание текста: **left** или **center**?
- [ ] Выравнивание кнопок: **left** или **center**?
- [ ] Иконки/изображения: **margin: auto** для центрирования

### Layouts
- [ ] **Staggered layouts**: есть ли смещения элементов по вертикали? (Services лесенка)
- [ ] Порядок секций: проверить **точный порядок** по Figma

### ⚠️ КРИТИЧНО: Анализ структуры секции перед кодом
**ПЕРЕД написанием HTML/CSS внимательно проанализируй Figma:**

1. **Получи screenshot** секции: `get_screenshot nodeId=XXX:XXX`
2. **Изучи координаты** элементов в `get_design_context`:
   - `top` - вертикальная позиция (что ВЫШЕ, что НИЖЕ)
   - `left` - горизонтальная позиция (что СЛЕВА, что СПРАВА)
3. **Определи layout**:
   - Элементы на ОДНОМ уровне `top` = **flexbox row** (рядом)
   - Элементы с РАЗНЫМ `top` = **вертикальная структура** (друг под другом)
4. **НЕ УГАДЫВАЙ** - если не уверен, сделай screenshot и проанализируй визуально

**Пример ошибки:** Hero V3 - sidebar казался рядом с баннером, но по координатам был НИЖЕ (top: 969px vs banner top: 188px). Правильная структура: баннер сверху, sidebar+image снизу.

### ⚠️ Проверка вариантов секций
**ПЕРЕД использованием шаблона проверь:**

1. **Какие флаги требуются** для отображения элементов:
   - Пример: `steps-4.html` требует `ctaBottom: true` или `ctaTop: true` чтобы показать заголовок
   - Пример: `cta-v1.html` - `layoutRow: true` для layout "текст слева, кнопка справа"

2. **Все ли данные заполнены**:
   - Если `ctaText: ""` пустой, кнопка не покажется
   - Если флаг не установлен, секция может отрисоваться неполностью

3. **Сравни с Figma** - есть ли:
   - Заголовок секции?
   - CTA кнопка? Где она - сверху/снизу/справа?
   - Какой текст на кнопке?

**Примеры ошибок:**
- Steps без `ctaBottom: true` → заголовок не показывается
- CTA с `layoutRow` но без флага → кнопка по центру вместо справа

### ⚠️ КРИТИЧНО: Верификация ВСЕХ текстов по Figma
**НИКОГДА не копируй тексты из других лендингов или по памяти!**

1. **Для КАЖДОЙ секции** получи данные из Figma: `get_design_context nodeId=XXX:XXX`
2. **Скопируй тексты ДОСЛОВНО** из Figma, включая:
   - Заголовки
   - Описания
   - Тексты кнопок
   - Подписи
3. **Сравни символ за символом** - даже мелкие различия важны:
   - "Bilder aussuchen" ≠ "Bildauswahl"
   - "Zum Fotoshooting kommen" ≠ "Let's start: Fotoshooting"

**Пример ошибки:** Steps Landing 3 - все 4 текста описаний были неправильные, скопированы из другого лендинга вместо Figma.

### ⚠️ КРИТИЧНО: Разные компоненты с похожими названиями

**Одинаковое название секции ≠ одинаковый компонент!**

Перед использованием существующего шаблона, ОБЯЗАТЕЛЬНО проверь в Figma:
1. **Тип компонента** - аккордеон, карточки, список?
2. **Интерактивность** - есть раскрытие/закрытие или статика?
3. **Layout** - сколько колонок, какая сетка?

**Примеры различий между лендингами:**

| Секция | Landing 1/2 | Landing 3 |
|--------|-------------|-----------|
| FAQ | Аккордеон (раскрываются) | Статичные карточки 2x2 |
| Features | 3 колонки вертикально | 2x2 сетка (иконка + текст inline) |
| Gallery | Strip (4 в ряд) или Slider | Decorated (с декоративными элементами) |
| CTA | Центрированная | Row layout (текст слева, кнопка справа) |

**Пример ошибки:** FAQ Landing 3 - использовал faq-accordion.html (раскрывающийся), а в Figma это статичные карточки без интерактивности. Нужен отдельный шаблон faq-cards.html.

### ⚠️ КРИТИЧНО: Проверяй выравнивание текста в Figma

**ВСЕГДА проверяй text-align для каждой секции!**

Заголовки могут быть выровнены по-разному в разных лендингах:
- Landing 1/2: `text-align: left`
- Landing 3: `text-align: center`

**Что проверять:**
1. Заголовки секций (h2, h3)
2. Подзаголовки
3. Текст в карточках
4. Кнопки CTA

**Пример ошибки:** Services Landing 3 - заголовок "Das ganze Jahr über..." должен быть по центру, а был слева.

### ⚠️ КРИТИЧНО: Проверяй стиль кнопок в Figma

**Кнопки могут быть разных типов в разных лендингах!**

| Параметр | Landing 1/2 | Landing 3 |
|----------|-------------|-----------|
| Тип | `btn-outline` (контур) | `btn-primary` (заливка) |
| Ширина | 448px | 473px |
| Высота | 60px | 70px |
| Radius | 0 | 30px |
| Позиция | Слева | По центру |

**Что проверять для каждой кнопки:**
1. Заливка или контур?
2. Точные размеры (width, height)
3. Border-radius
4. Выравнивание (left/center)

**Пример ошибки:** Services Landing 3 - использовал btn-outline 448x60px слева, а должен быть btn-primary 473x70px по центру с radius 30px.

### ⚠️ КРИТИЧНО: Проверяй наличие/отсутствие элементов в секции

**Одна и та же секция может иметь разные элементы в разных лендингах!**

| Элемент | Landing 1/2 | Landing 3 |
|---------|-------------|-----------|
| About: Divider line | ДА | НЕТ |
| About: Label (маленький) | ДА | НЕТ (часть заголовка) |
| About: Social icons | ДА | НЕТ |
| FAQ: Expand/collapse | ДА | НЕТ (статичные карточки) |

**Что проверять:**
1. Есть ли разделительные линии?
2. Есть ли отдельные label/подзаголовки?
3. Есть ли социальные иконки?
4. Есть ли интерактивные элементы?

**Пример ошибки:** About Landing 3 - использовал шаблон с divider и отдельным label, а в Figma это compact вариант без линии, с объединённым заголовком.

### ⚠️ КРИТИЧНО: Проверяй выравнивание элементов по высоте

**Текст и изображения могут быть выровнены по высоте!**

Способ проверки в Figma:
1. Найди Y координату верха элемента (top)
2. Найди высоту элемента (height)
3. Вычисли Y координату низа: top + height
4. Сравни низ текста и низ изображения

**Пример из Landing 3 About:**
- Изображение: top=7263, height=608 → низ на 7871px
- Текст: top=7410, height=461 → низ на 7871px
- Они заканчиваются на одном уровне!

**CSS решение:**
```css
.about--rounded .about__image {
  width: 551px;
  height: 608px; /* Точный размер из Figma */
}
```

### ⚠️ КРИТИЧНО: Проверяй фоны секций в Figma

**Фоны секций могут быть РАЗНЫМИ в разных лендингах!**

| Лендинг | Фоны секций |
|---------|-------------|
| Landing 1/2 | Чередование primary (#F5EDE0) и secondary (#EEE3D0) |
| Landing 3 | ВСЕ секции имеют одинаковый primary (#F5EDE0) фон |
| Landing 4 | Чередование (см. таблицу в разделе Landing 4) |

**Как реализовано:**
Шаблоны поддерживают параметр `bgClass` для переопределения фона:
```json
{
  "template": "steps/steps-4.html",
  "data": {
    "bgClass": "section--bg-primary",  // переопределяет default secondary
    "rounded": true
  }
}
```

**Шаблоны с конфигурируемым фоном:**
- `steps/steps-4.html` - default: `section--bg-secondary`
- `steps/steps-v4.html` - default: `section--bg-primary`
- `cta/cta-v1.html` - default: `section--bg-secondary`
- `services/services-grid.html` - default: `section--bg-secondary`
- `faq/faq-accordion.html` - default: `section--bg-primary`
- `gallery/gallery-fullwidth.html` - default: `section--bg-primary`
- `gallery/gallery-slider.html` - default: `section--bg-primary`

**Что проверять:**
1. Какой цвет фона у КАЖДОЙ секции в Figma?
2. Чередуются ли фоны или все одинаковые?
3. Добавить `bgClass` в JSON если фон отличается от default

**Пример ошибки:** Landing 3 - все секции использовали дефолтные фоны (чередование), а в Figma ВСЯ страница имеет единый фон #F5EDE0.

### Контент
- [ ] Социальные иконки: **показывать** или **скрыть**?
- [ ] Текст: **один блок** или **массив параграфов**?
- [ ] Размеры изображений: **точные значения** из Figma

### Поведение
- [ ] **Скролл** при загрузке страницы: НЕ скроллить автоматически
- [ ] **Квиз**: показывается только после клика на CTA

### Мобильная версия (ОБЯЗАТЕЛЬНО проверять!)
- [ ] **Кнопки**: на мобильных (`< 768px`) ВСЕ кнопки должны быть `width: 100%`
- [ ] **Отступы**: между кнопкой и следующим элементом минимум `margin-bottom: 30px`
- [ ] **Изображения**: проверить что не налезают на текст/кнопки
- [ ] **Формы**: поля ввода должны быть `max-width: 100%`
- [ ] **Тексты**: проверить что не обрезаются и читаемы

### ⚠️ КРИТИЧНО: Мобильные проблемы с кнопками

**Главная проблема:** В `base.css` кнопки имеют `white-space: nowrap` - текст не переносится!

**Длинные немецкие тексты кнопок (Landing 3):**
- "Hier geht's zu den Gutscheinen" (~30 символов)
- "Kostenloses Fotoshooting sichern" (~32 символа)
- "Jetzt kostenloses Fotoshooting sichern" (~38 символов)

**Решение в `responsive.css` (< 768px):**
```css
.btn {
  white-space: normal;  /* Разрешаем перенос */
  height: auto;         /* Убираем фиксированную высоту */
  min-height: 55px;     /* Минимальная высота */
  padding: 12px 20px;   /* Вертикальные отступы */
  line-height: 1.3;     /* Межстрочный интервал */
}
```

**Breakpoints для кнопок:**
| Breakpoint | min-height | font-size |
|------------|------------|-----------|
| < 768px | 55px | 15px |
| < 576px | 48-50px | 14px |

### ⚠️ КРИТИЧНО: Мобильные размеры шрифтов

**Типичные размеры для мобильных:**
| Элемент | Desktop | Mobile (< 768px) | Small (< 576px) |
|---------|---------|------------------|-----------------|
| h1/h2 заголовок | 45px | 22-26px | 20-22px |
| h3/h4 подзаголовок | 28px | 18-20px | 16-18px |
| Текст параграфа | 22px | 15-16px | 14-15px |
| Кнопка | 22px | 15px | 14px |

**Пример ошибки:** Landing 3 - тексты на мобильных были 22px (как на десктопе), из-за чего текст выглядел огромным и не помещался

### Размеры (Landing 1)
| Элемент | Размер |
|---------|--------|
| Promo image | 428x440px (`shortImage: true`) |
| Hero main image | 428x394px |
| Hero secondary image | 306x290px |
| Gallery fullwidth | 100% x 656px |
| Service card image | 300x300px |
| About image | 466x560px |

### Варианты компонентов
- `promo__image` - 428x533px (Landing 2)
- `promo__image--short` - 428x440px (Landing 1)
- `gallery-slider` - слайдер с 3 видимыми изображениями
- `gallery-fullwidth` - полноширинное изображение
- `gallery-strip` - 4 статичных изображения в ряд

---

## ⚠️ Верификация текстов квизов

**ОБЯЗАТЕЛЬНО** перед созданием/обновлением квизов:

1. Получить данные из Figma для каждого шага:
   - `Figma:get_design_context nodeId=XXX:XXX`

2. Сверить **точно** следующие поля:
   - `progress` - текст прогресса
   - `question` - вопрос
   - `options[].text` - тексты вариантов ответов

3. Копировать тексты **дословно**, включая:
   - Пробелы вокруг дефисов
   - Знаки препинания
   - Регистр букв

### Figma Node IDs для квизов

| Landing | Step 1 | Step 2 | Step 3 | Step 4 | Form | Success |
|---------|--------|--------|--------|--------|------|---------|
| 1 | 137:51 | 137:159 | 137:71 | 137:179 | 137:111 | 137:84 |
| 2 | 313:26 | 313:82 | 313:47 | 313:68 | 313:100 | 313:135 |
| 3 | 370:441 | 370:460 | 370:479 | 370:503 | 370:526 | 370:560 |
| 4 | 483:63 | 483:85 | 483:100 | 483:122 | 483:144 | 483:180 |

---

## 📋 Результаты верификации квизов

### Landing 3 (Hunde-Fotoshooting) - Верификация 2026-01-18

**Статус: ЗАВЕРШЕНО ✅**

| Шаг | Статус | Примечание |
|-----|--------|------------|
| Step 1 | ✅ OK | Тексты соответствуют |
| Step 2 | ✅ ИСПРАВЛЕНО | "pelzigen" → "pelzige" |
| Step 3 | ✅ OK | Тексты соответствуют |
| Step 4 | ✅ OK | Тексты соответствуют |
| Form | ✅ ИСПРАВЛЕНО | title + note |
| Success | ✅ ИСПРАВЛЕНО | Данные фотографа |

**Исправленные несоответствия:**

1. **Step 2, Option 2:**
   - Было: "Meine pelzigen Freunde"
   - Стало: "Meine pelzige Freunde"

2. **Form - formTitle:**
   - Было: "Trage dich jetzt ein und sichere dir dein Platz für ein"
   - Стало: "Trag dich jetzt ein und sichere dir dein kostenloses Hunde-Fotoshooting!"

3. **Form - formNote:**
   - Было: "" (пусто)
   - Стало: "Das Fotoshooting findet nur bis zum 23.07.2025 statt! Schnell sein lohnt sich - ich freue mich auf euch!"

4. **Success - aboutName/aboutTagline:**
   - Было: "Möst Fotografie" / "Tierfotografie mit Herz"
   - Стало: "Yvonne Jadke – Fotograf Hannover" / "Familienfotos, Businessportraits & Hochzeitsfotografie"

5. **Success - aboutTexts:**
   - Обновлены тексты по Figma

**Регрессия:** Landing 1, 2, 4 - не затронуты ✅

---

## 🎯 Landing 4 (Kinder-Fotoshooting) - Особенности

### Уникальные компоненты Landing 4

| Компонент | Шаблон | Особенность |
|-----------|--------|-------------|
| Hero | `hero-v4.html` | Лого ВНУТРИ тёмной секции, не отдельный header |
| Content | `content-v2.html` | 2 колонки: заголовок+кнопка слева, текст справа |
| Steps | `steps-v4.html` | 3 вертикальные карточки + изображение справа |
| CTA | `cta-v1.html` + `alignLeft` | Левое выравнивание (не центр) |

### ⚠️ КРИТИЧНО: Hero V4 - Лого внутри секции

**Landing 4 НЕ имеет отдельного header!**

Лого находится ВНУТРИ тёмной hero секции:
```json
{
  "template": "hero/hero-v4.html",
  "data": {
    "logoText": "Logo",  // Лого внутри hero, НЕ отдельный header
    "label": "Kostenloses",
    ...
  }
}
```

**Figma:** Лого (node 479:32) - `top: 20px, left: 46px` на фоне `#3D3D3D`

### ⚠️ КРИТИЧНО: Content V2 - Кнопка в левой колонке

**Кнопка должна быть ПОД заголовком в левой колонке, НЕ отдельно внизу!**

Figma координаты (node 479:47):
- Заголовок: `top: 1394px, left: 46px`
- Кнопка: `top: 1491px, left: 47px`
- Текст: `top: 1394px, left: 50%+15px` (правая колонка)

**CSS:** `.content-section__btn { margin-top: 40px; }`

### ⚠️ КРИТИЧНО: CTA - Левое выравнивание

**Landing 4 CTA выровнен по ЛЕВОМУ краю, не по центру!**

```json
{
  "template": "cta/cta-v1.html",
  "data": {
    "rounded": true,
    "alignLeft": true  // ← Обязательно для Landing 4!
  }
}
```

**CSS:** `.cta--left` переопределяет `text-align: center` на `text-align: left`

**Отступы в CTA (FIGMA node 479:117):**
- Title → Subtitle: 0px (subtitle сразу после title)
- Subtitle → Button: 31px (`margin-top: 31px` на кнопке)

### ⚠️ КРИТИЧНО: Фоны секций Landing 4

**Landing 4 имеет ЧЕРЕДУЮЩИЕСЯ фоны (не как Landing 3)!**

| Секция | Фон | bgClass в конфиге |
|--------|-----|-------------------|
| Hero (dark) | #3D3D3D | — (в шаблоне) |
| Hero gallery | #F5EDE0 | — (primary, default) |
| Content-v2 | #F5EDE0 | — (primary, default) |
| CTA | #EEE3D0 | — (secondary, default) |
| Steps-v4 | #F5EDE0 | — (primary, default) |
| FAQ | #EEE3D0 | `"bgClass": "section--bg-secondary"` ⚠️ |
| Gallery slider | #F5EDE0 | — (primary, default) |
| Services | #EEE3D0 | `"bgClass": "section--bg-secondary"` |
| Gallery fullwidth | #F5EDE0 | — (primary, default) |
| About | #F5EDE0 | — (primary, default) |
| Footer | #FFFFFF | — (white, в шаблоне) |

### Флаги для шаблонов Landing 4

| Шаблон | Флаг | Назначение |
|--------|------|------------|
| `hero-v4.html` | `logoText` | Показать лого внутри hero |
| `cta-v1.html` | `alignLeft` | Левое выравнивание |
| `cta-v1.html` | `rounded` | Скруглённые кнопки |
| `faq-accordion.html` | `bgClass` | Переопределить фон (secondary для L4) |
| `faq-accordion.html` | `cardStyle: "rounded"` | Левая кнопка 432x60px, radius 40px |
| `steps-v4.html` | — | 3 шага слева, картинка+кнопка справа (кнопка ПОД картинкой!) |
| `content-v2.html` | — | Кнопка в левой колонке |

### 📋 Landing 4 — Полный конфиг секций (копируй!)

```json
// Hero - БЕЗ отдельного header!
{ "template": "hero/hero-v4.html", "data": { "logoText": "Logo", ... } }

// Content - кнопка в ЛЕВОЙ колонке
{ "template": "content/content-v2.html", "data": { ... } }

// CTA - ЛЕВОЕ выравнивание
{ "template": "cta/cta-v1.html", "data": { "rounded": true, "alignLeft": true, ... } }

// Steps - кнопка ПОД картинкой справа
{ "template": "steps/steps-v4.html", "data": { ... } }

// FAQ - фон SECONDARY + rounded
{ "template": "faq/faq-accordion.html", "data": { "bgClass": "section--bg-secondary", "cardStyle": "rounded", ... } }

// Gallery slider - primary фон (default OK)
{ "template": "gallery/gallery-slider.html", "data": { "rounded": true, ... } }

// Services - фон SECONDARY
{ "template": "services/services-grid.html", "data": { "bgClass": "section--bg-secondary", "rounded": true, ... } }

// About - primary фон (default OK)
{ "template": "about/about-v1.html", "data": { "imagesRight": true, ... } }
```

### ⚡ Быстрая проверка Landing 4

```bash
# Проверь эти классы в сгенерированном HTML:
grep "section--bg-secondary" dist/landing4-kids/index.html  # CTA, FAQ, Services
grep "cta--left" dist/landing4-kids/index.html              # CTA левое выравнивание
grep "faq--rounded" dist/landing4-kids/index.html           # FAQ rounded кнопка
grep "hero__logo" dist/landing4-kids/index.html             # Лого внутри hero
```
